# Security audit results (nunca commitear scan data)
security-audit/
audit-results/
scan-*/

# Test repositories and data
test-*/
*-test/
test.json
quick-audit.json

# Temporary files and backups
*.backup
*.before-*
*.tmp
temp_*

# Build artifacts and downloads
*.tar.gz
*.zip

# Environment files with potential secrets
.env
.env.local

# OS generated files
.DS_Store
Thumbs.db

# Security scan outputs (never commit scan results)
security-report.*
audit-results.*
